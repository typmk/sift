#!/usr/bin/env bb
(require '[babashka.classpath :as cp] '[babashka.fs :as fs])
(cp/add-classpath (str (fs/parent (fs/parent (fs/real-path *file*))) "/src"))
(require 'net.typemark.sift.cli)
(apply net.typemark.sift.cli/-main *command-line-args*)
